spring:
  application:
    name: scm2.0
  datasource:
    url: jdbc:mysql://localhost:3306/app_db
    username: app_user
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080

